# libSQL database. Use a local file in development, a Turso URL in production.
DATABASE_URL="file:local.db"

# Required only for a remote (libsql://) database. Leave empty for a local file.
DATABASE_AUTH_TOKEN=""

# Absolute origin of this deployment — canonical links and Open Graph tags.
PUBLIC_SITE_URL="http://localhost:5173"
